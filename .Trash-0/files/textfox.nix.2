{
  config,
  pkgs,
  inputs,
  ...
}: {
  imports = [
    inputs.textfox.homeManagerModules.default
  ];

  programs.firefox = {
    enable = true;
    package = pkgs.librewolf;
  };

  textfox = {
    enable = true;
  };
}
